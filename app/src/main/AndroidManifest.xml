<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zivko.shoppinglist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_shopplist"
        android:label="ShoppingList"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name="com.example.zivko.shoppinglist.ListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.zivko.shoppinglist.DeatailActivity">

        </activity>

        <activity android:name="com.example.zivko.shoppinglist.Settings">

        </activity>

    </application>

</manifest>